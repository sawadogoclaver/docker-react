sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t revalc/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true revalc/docker-react npm run test -- --coverage
